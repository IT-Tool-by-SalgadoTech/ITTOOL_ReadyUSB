#NOTE IT-Tool - Launch Script_Saver (Linux)
#NOTE Runs Script_Saver_Linux.sh directly from GitHub, 3-distro safe, keeps stdin for interactive prompts
WTIME 800
SCRIPT L="https://github.com/IT-Tool-by-SalgadoTech/ReaduUSB_With_PS1/raw/refs/heads/main/ReadyUSB/B.Script_Saver_Runner/Script_Saver_Linux.sh"; command -v curl >/dev/null 2>&1 || command -v wget >/dev/null 2>&1 || { command -v apt-get >/dev/null 2>&1 && apt-get install -y curl; command -v dnf >/dev/null 2>&1 && dnf install -y curl; command -v pacman >/dev/null 2>&1 && pacman -Sy --noconfirm curl; }; if command -v curl >/dev/null 2>&1; then bash <(curl -fsSL "$L"); else bash <(wget -qO- "$L"); fi
ENTER
