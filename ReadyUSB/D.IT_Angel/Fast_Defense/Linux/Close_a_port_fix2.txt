#NOTE IT-Tool - Close a port (firewall) (Linux) - Fast Defense launcher
#NOTE Installs deps (Debian/Fedora/Arch), downloads Close_a_port_fix2.sh (+ _itlib.sh) to ~/ITTOOL, runs it
WTIME 800
SCRIPT U="${SUDO_USER:-$USER}"; H=$(getent passwd "$U"|cut -d: -f6); D="$H/ITTOOL"; F="$D/Close_a_port_fix2.sh"; L="https://github.com/IT-Tool-by-SalgadoTech/ReaduUSB_With_PS1/raw/refs/heads/main/ReadyUSB/D.IT_Angel/Fast_Defense/Windows/Block_Windows_MAC.ps1"; LIB="$D/_itlib.sh"; LL="https://github.com/IT-Tool-by-SalgadoTech/ReaduUSB_With_PS1/raw/refs/heads/main/ReadyUSB/D.IT_Angel/Fast_Defense/Linux/_itlib.sh"; mkdir -p "$D"; command -v curl >/dev/null 2>&1 || command -v wget >/dev/null 2>&1 || { command -v apt-get >/dev/null 2>&1 && apt-get install -y curl; command -v dnf >/dev/null 2>&1 && dnf install -y curl; command -v pacman >/dev/null 2>&1 && pacman -Sy --noconfirm curl; }; if command -v curl >/dev/null 2>&1; then curl -fsSL "$L" -o "$F"; curl -fsSL "$LL" -o "$LIB"; else wget -qO "$F" "$L"; wget -qO "$LIB" "$LL"; fi; chmod +x "$F"; chown "$U": "$D" "$F" "$LIB" 2>/dev/null; command -v iptables >/dev/null 2>&1 || { command -v apt-get >/dev/null 2>&1 && apt-get install -y iptables; command -v dnf >/dev/null 2>&1 && dnf install -y iptables; command -v yum >/dev/null 2>&1 && yum install -y iptables; command -v pacman >/dev/null 2>&1 && pacman -Sy --noconfirm iptables; }; bash "$F"
ENTER
