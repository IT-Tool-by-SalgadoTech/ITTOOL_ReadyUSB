#NOTE IT-Tool - Bluetooth_List_Paired (Linux)
#NOTE Downloads 436.Bluetooth_List_Paired.sh to ITTOOL in user home, 3-distro safe, runs it
WTIME 800
SCRIPT U="${SUDO_USER:-$USER}"; H=$(getent passwd "$U"|cut -d: -f6); D="$H/ITTOOL"; F="$D/436.Bluetooth_List_Paired.sh"; L="https://github.com/IT-Tool-by-SalgadoTech/ReaduUSB_With_PS1/raw/refs/heads/main/ReadyUSB/A.OS_System/B.Linux/B.B__Networks/B.B.D__Bluetooth/436.Bluetooth_List_Paired.sh"; mkdir -p "$D"; command -v curl >/dev/null 2>&1 || command -v wget >/dev/null 2>&1 || { command -v apt-get >/dev/null 2>&1 && apt-get install -y curl; command -v dnf >/dev/null 2>&1 && dnf install -y curl; command -v pacman >/dev/null 2>&1 && pacman -Sy --noconfirm curl; }; if command -v curl >/dev/null 2>&1; then curl -fsSL "$L" -o "$F"; else wget -qO "$F" "$L"; fi; chmod +x "$F"; chown "$U": "$D" "$F" 2>/dev/null; command -v bluetoothctl >/dev/null 2>&1 || { command -v apt-get >/dev/null 2>&1 && apt-get install -y bluez; command -v dnf >/dev/null 2>&1 && dnf install -y bluez; command -v pacman >/dev/null 2>&1 && pacman -Sy --noconfirm bluez-utils; }; bash "$F"
ENTER
